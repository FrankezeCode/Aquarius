/**
 * Aave protocol adapters / web3 helpers.
 * TODO: Implement real adapter logic.
 */

export async function fetchAaveMetrics() {
  // Placeholder — wire real API/contract calls
  return {
    tvl: null,
    utilization: null,
    liquidations24h: null,
    riskLevel: null,
  };
}

export async function fetchAaveOpportunities() {
  // Placeholder — wire real data
  return [];
}

export async function fetchAaveActivity() {
  // Placeholder — wire real event stream
  return [];
}
